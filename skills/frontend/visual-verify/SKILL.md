---
name: visual-verify
description: >
  Alias for browser-verify (Playwright + DevTools proof). Prefer loading
  browser-verify. Use when user or older docs say visual-verify, Playwright
  snapshots, or axe e2e proof. Prefer quality-check when QA path is unclear.
---

# visual-verify → browser-verify

This skill was **merged into [`browser-verify`](../browser-verify/SKILL.md)**.

**Open `browser-verify` now** and follow mode **A (harness / Playwright)** or **B (DevTools)**.

Do not keep a parallel workflow here.
